import React, {useContext, useEffect} from "react";

import { QuizContext } from '../../reducers';

// Data
import quizData from '../../data/quiz.json';

const QuizResult = (theme, questions) => {

    return (
        <span>QuizResult</span>
    )
}

export default Quiz;